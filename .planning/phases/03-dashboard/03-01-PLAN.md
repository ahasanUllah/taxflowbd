---
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/Sidebar.tsx
  - src/components/dashboard/Header.tsx
  - src/app/dashboard/layout.tsx
autonomous: true
requirements: []
---

## Objective

Create the authenticated dashboard layout foundation: a server-side layout that guards all `/dashboard/*` routes, a client-side Sidebar with mobile drawer and 3 nav links, and a Header component showing the user's Bangla greeting.

## Tasks

<task id="1">
<title>Create Sidebar component</title>
<read_first>
- src/components/shared/LogoutButton.tsx
- src/app/globals.css
- .planning/codebase/CONVENTIONS.md
- .planning/phases/03-dashboard/03-CONTEXT.md
</read_first>
<action>
Create `src/components/dashboard/Sidebar.tsx` as a client component.

- `"use client"` on line 1
- Import: `useState` from react, `usePathname` from `next/navigation`, `Link` from `next/link`, `LogoutButton` from `@/components/shared/LogoutButton`
- `useState<boolean>(false)` named `isOpen` with setter `setIsOpen`
- Nav links array (defined outside the component): `[{ href: "/dashboard", label: "ড্যাশবোর্ড" }, { href: "/dashboard/return/interview", label: "ট্যাক্স রিটার্ন" }, { href: "/dashboard/profile", label: "আমার প্রোফাইল" }]`
- Desktop: `hidden md:flex flex-col w-60 h-screen bg-white border-r border-gray-100 fixed top-0 left-0 z-20`
- Mobile overlay: when `isOpen`, render a full-screen backdrop `div` (onClick closes drawer) and a sliding panel `div` with class `fixed inset-y-0 left-0 z-30 w-64 bg-white shadow-xl flex flex-col`
- Hamburger button: `md:hidden` visibility, toggles `isOpen`; use 3 horizontal bars (text or SVG)
- Nav links: each `<Link>` gets active highlight if `pathname === href` — active: `bg-brand-surface text-brand-primary font-semibold`, inactive: `text-gray-600 hover:bg-gray-50`
- Link class (both states share): `flex items-center gap-3 px-4 py-3 rounded-xl text-sm transition`
- Logout pinned to bottom: in both desktop and mobile panels, use `mt-auto` container and render `<LogoutButton className="w-full text-left px-4 py-3 rounded-xl text-sm text-gray-600 hover:bg-red-50 hover:text-brand-accent transition" />`
- Default export: `Sidebar`
</action>
<acceptance_criteria>
- File starts with `"use client"`
- Imports `LogoutButton` from `@/components/shared/LogoutButton`
- Renders exactly 3 `<Link>` elements with hrefs `/dashboard`, `/dashboard/return/interview`, `/dashboard/profile`
- Bangla labels: ড্যাশবোর্ড, ট্যাক্স রিটার্ন, আমার প্রোফাইল
- `isOpen` state controls mobile drawer visibility
- Desktop sidebar has `md:flex` and `hidden` to show only on md+ screens
- Hamburger has `md:hidden` class
- LogoutButton rendered in both desktop and mobile panels with `mt-auto` spacing
</acceptance_criteria>
</task>

<task id="2">
<title>Create Header component</title>
<read_first>
- src/app/globals.css
- .planning/phases/03-dashboard/03-CONTEXT.md
- .planning/codebase/CONVENTIONS.md
</read_first>
<action>
Create `src/components/dashboard/Header.tsx` as a server-compatible component (no hooks, no "use client").

- Props interface: `interface HeaderProps { userName: string }`
- Derive `firstName`: `const firstName = userName.split(" ")[0] || "ব্যবহারকারী"`
- Outer: `<header className="flex items-center justify-between h-16 px-6 bg-white border-b border-gray-100 sticky top-0 z-10">`
- Left: `<h1 className="text-lg font-semibold text-foreground">ড্যাশবোর্ড</h1>`
- Right: `<p className="text-sm text-gray-600">স্বাগত, <span className="font-medium text-brand-primary">{firstName}</span>!</p>`
- Default export: `Header`
</action>
<acceptance_criteria>
- No `"use client"` directive
- Props: `userName: string`
- Renders `স্বাগত,` followed by first name extracted from `userName.split(" ")[0]`
- Falls back to `ব্যবহারকারী` if userName is empty
- Header is a `<header>` element with sticky positioning
</acceptance_criteria>
</task>

<task id="3">
<title>Create dashboard layout</title>
<read_first>
- src/lib/auth.ts
- src/components/dashboard/Sidebar.tsx
- src/components/dashboard/Header.tsx
- .planning/codebase/CONVENTIONS.md
- .planning/phases/03-dashboard/03-CONTEXT.md
</read_first>
<action>
Create `src/app/dashboard/layout.tsx` as a server component.

- No `"use client"` directive
- Imports: `auth` from `@/lib/auth`, `{ headers }` from `next/headers`, `{ redirect }` from `next/navigation`
- Imports: `Sidebar` from `@/components/dashboard/Sidebar`, `Header` from `@/components/dashboard/Header`
- Function signature: `export default async function DashboardLayout({ children }: { children: React.ReactNode })`
- Auth check: `const session = await auth.api.getSession({ headers: await headers() })`; if `!session`, call `redirect("/login")`
- Layout structure:
  ```
  <div className="flex h-screen bg-brand-surface">
    <Sidebar />
    <div className="flex-1 flex flex-col md:ml-60 overflow-hidden">
      <Header userName={session.user.name ?? ""} />
      <main className="flex-1 overflow-y-auto">
        {children}
      </main>
    </div>
  </div>
  ```
- The `md:ml-60` on the content wrapper offsets it by the sidebar width on desktop; on mobile, sidebar is an overlay so no offset
</action>
<acceptance_criteria>
- No `"use client"` directive
- Calls `auth.api.getSession({ headers: await headers() })`
- Calls `redirect("/login")` when session is null/undefined
- Passes `session.user.name ?? ""` to `<Header userName={...} />`
- Renders `<Sidebar />` and `<main>` wrapping `{children}`
- Main content area has `md:ml-60` to account for fixed sidebar on desktop
</acceptance_criteria>
</task>

## Verification

- `npx tsc --noEmit` passes with 0 errors on these 3 files
- Visiting `/dashboard` while unauthenticated (no session cookie) redirects to `/login`
- Sidebar shows 3 nav links with correct Bangla labels

## must_haves

truths:
- Dashboard layout calls `auth.api.getSession` server-side and redirects unauthenticated users — double-auth rule (proxy.ts + layout.tsx)
- Sidebar is client component with `"use client"` — it uses useState and usePathname
- Header is a server-compatible component — no hooks, receives userName as prop
- All user-facing text is in Bangla (ড্যাশবোর্ড, ট্যাক্স রিটার্ন, আমার প্রোফাইল, লগআউট করুন)
